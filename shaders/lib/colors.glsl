#if !defined(COLORS_GLSL)
#define COLORS_GLSL

const vec3 SUN_COLOR = pow(vec3(0.734, 0.583, 0.377), vec3(2.2)) * 100.0;
const vec3 SKY_COLOR = pow(vec3(0.21, 0.25, 0.59), vec3(2.2));

#endif // COLORS_GLSL